<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Smart Button to redirect meeting URL and boolean field to enable Meet Meeting Link -->
        <record id="view_calendar_meet_link" model="ir.ui.view">
            <field name="name">calendar.meet_link.form</field>
            <field name="model">calendar.event</field>
            <field name='inherit_id' ref='calendar.view_calendar_event_form'/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_join_video_call']"
                       position="after">
                    <button name="action_redirect_link"
                            class="btn btn-link ps-0"
                            help="Join Meet Meeting" type="object"
                            invisible="not google_meeting_url">
                        <span class="oi oi-arrow-right"/>
                        <span>Join Meet Meeting</span>
                    </button>
                </xpath>
                <xpath expr="//field[@name='categ_ids']"
                       position="before">
                    <field name="google_meeting_url"
                           widget="CopyClipboardChar"/>
                </xpath>
                <xpath expr="//page[@name='page_invitations']"
                       position="before">
                    <page name="meet_meeting" string="Google Meet">
                        <h1>Google Meet</h1>
                        <group name="page_meet_link">
                            <field name="google_link_check"/>
                            <field name="meet_warning_check"
                                   invisible="1"/>
                            <field name="description"/>
                        </group>
                        <span style="color: red;"
                              invisible="not meet_warning_check">
                            * Meet Meeting will be deleted by disabling it.
                        </span>
                    </page>
                </xpath>
                <xpath expr="//field[@name='description']" position="replace">
                </xpath>
            </field>
        </record>
    </data>
</odoo>
