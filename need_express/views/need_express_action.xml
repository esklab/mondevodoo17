<odoo>
    <!-- Action serveur -->
    <record id="action_server_create_po_from_need_express" model="ir.actions.server">
        <field name="name">Créer bon de commande</field>
        <field name="model_id" ref="model_need_express"/>
        <field name="binding_model_id" ref="model_need_express"/>
        <field name="binding_view_types">list</field> <!-- Action contextuelle -->
        <field name="state">code</field>
        <field name="code">
            action = env['need.express'].create_purchase_order_from_lines(records)
        </field>
    </record >
</odoo>
